import 'package:chemfriend/chemistry/chemistry.dart';

void main() {
  print(Element.from('H').category);
  /*
	Equation f = Equation('Cl2(g) + GaF3(aq)');
	print('Reaction Type: ${typeToString[Equation.getType(f.reactants)]}');
	f.solve();
	print(f.toString());
  */
}
